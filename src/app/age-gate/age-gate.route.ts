import { Route } from '@angular/router';
import { AgeGateComponent } from './age-gate.component';

export const AgeGateRoutes: Route[] = [
    {
        path: 'age',
        component: AgeGateComponent,
    },
];